[Unit]
Description=Workshop Metrics (system aggregates, 15s)
After=postgresql.service network.target

[Service]
Type=oneshot
User=$SERVER_USER
WorkingDirectory=$WORKSHOP_DIR
ExecStart=/usr/bin/env npx tsx scripts/collect-system.ts
TimeoutStartSec=20
